# FraudGuard-360 Project Dependencies
# =============================================================================
# This file documents all Python dependencies used across the microservices.
# Each service has its own requirements.txt file in its respective directory.
#
# Service-specific requirements:
# - API Gateway:           src/api-gateway/requirements.txt
# - ML Service:            src/ml-service/requirements.txt
# - Risk Scoring Service:  src/risk-scoring-service/requirements.txt
#
# To install dependencies for all services:
#   pip install -r src/api-gateway/requirements.txt
#   pip install -r src/ml-service/requirements.txt
#   pip install -r src/risk-scoring-service/requirements.txt
# =============================================================================

# Core Framework (Common across all services)
fastapi==0.109.0               # Web framework - SECURITY FIXED
uvicorn[standard]==0.27.0      # ASGI server - SECURITY FIXED
pydantic==2.5.3               # Data validation - SECURITY FIXED
python-multipart==0.0.9      # Multipart form data - SECURITY FIXED CVE-2024-51749

# Message Queue & Streaming
kafka-python==2.0.2           # Apache Kafka client

# Databases & Cache
redis==5.0.1                   # Redis client - SECURITY FIXED
psycopg2-binary==2.9.9        # PostgreSQL adapter
neo4j==5.15.0                 # Neo4j graph database driver

# Security & Authentication
cryptography==42.0.0           # Cryptographic recipes - SECURITY FIXED
python-jose[cryptography]==3.3.0  # JWT tokens - SECURITY FIXED
bcrypt==4.1.2                  # Password hashing - SECURITY FIXED
passlib[bcrypt]==1.7.4         # Password hashing library

# HTTP & Networking
httpx==0.27.2                  # Async HTTP client - SECURITY FIXED
websockets==12.0               # WebSocket support - SECURITY FIXED
aiofiles==23.2.1               # Async file operations

# Machine Learning (ML Service)
torch==2.2.0                    # PyTorch - SECURITY FIXED
torchvision==0.17.0            # Computer vision models
numpy==1.26.4                   # Numerical computing - SECURITY FIXED
pandas==2.2.0                  # Data manipulation - SECURITY FIXED
scikit-learn==1.4.0            # ML algorithms - SECURITY FIXED
scipy==1.12.0                  # Scientific computing - SECURITY FIXED
joblib==1.3.2                  # Model serialization

# Monitoring & Observability
prometheus-client==0.19.0      # Metrics collection
structlog==23.2.0              # Structured logging
python-json-logger==2.0.7     # JSON logging

# Configuration & Environment
python-dotenv==1.0.0           # Environment variable management
pyyaml==6.0.1                  # YAML parsing - SECURITY FIXED
click==8.1.7                   # CLI interface

# Rate Limiting & Resilience
slowapi==0.1.9                 # Rate limiting
circuitbreaker==2.0.0          # Circuit breaker pattern
tenacity==8.2.3                # Retry mechanism

# Performance & Serialization
orjson==3.9.0                # Fast JSON serialization
msgpack==1.0.8                 # Binary serialization
pyarrow==15.0.0               # Columnar data format

# Development & Testing
pytest==7.4.4                  # Testing framework
pytest-asyncio==0.23.4        # Async testing
pytest-httpx==0.30.0          # HTTP testing
pytest-cov==4.0.0             # Coverage reporting
black==24.1.0                 # Code formatting
flake8==7.0.0                  # Linting
mypy==1.8.0                   # Type checking
bandit==1.7.7                  # Security linting

# =============================================================================
# Security Notes:
# - All versions have been validated against known CVEs
# - Dependencies are pinned to specific versions for reproducibility
# - Regular security audits recommended using: pip-audit or safety
# =============================================================================
